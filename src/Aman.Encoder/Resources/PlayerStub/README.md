# Player stub goes here

This folder is empty in source control on purpose. Before you can use
"Export Package as EXE" in Aman.Encoder, build `src/Aman.Player` and copy
its published single-file output here as `Player.exe`.

`build/publish-player.ps1` does this for you automatically. See
`docs/BUILD.md` for the full sequence.
